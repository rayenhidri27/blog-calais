<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<div class="container">
		<a class="navbar-brand" href="#">Navbar</a>
		<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav mr-auto">
				<li class="nav-item active">
					<a class="nav-link" href="{{path('home')}}">Accueil
						<span class="sr-only">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="#">Link</a>
				</li>
			</ul>
			<ul class="nav navbar-nav navbar-right mr-3">
				<form class="form-inline" role="search" method="post" action="{{ path('recherche')}}">
					<div class="form-group">
						<input type="text" class="form-control mr-2" name="title" placeholder="Recherche...">
						<input type="date" class="form-control mr-2" name="date" placeholder="Choisir une date">
					</div>
					<button type="submit" class="btn btn-secondary mr-4">Chercher</button>
				</form>

				{% if app.user %}
					<li>
						<a href="{{ path('security_logout')}}" class="btn btn-danger ml-3">Déconnexion</a>
					</li>
				{% else %}
					<li>
						<a href="{{ path('security_register', {user_type:"user"})}}" class="btn btn-success">Inscription</a>
					</li>
					<li>
						<a href="{{ path('security_login')}}" class="btn btn-info ml-3">Connexion</a>
					</li>
				{% endif %}
			</ul>


		</div>
	</div>
</nav>
